"""The insights domains: metrics, events, goals, projects.

``metrics`` reads the series every build has; ``events``, ``goals`` and
``projects`` are the per-user tables and render empty without that mode.
"""
